# Changelog

## 1.0.0 - Initial migration from base extension. All functionality preserved from packages/extensions/landing-pages/ at parity, minus the `app-settings`-backed override for OpenRouter credentials (falls back to env-only, matching pre-migration behaviour). Store-config AI overrides are resolved from the host via string key.
