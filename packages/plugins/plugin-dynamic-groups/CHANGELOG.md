# Changelog

## 1.3.1

- Feature: consumir el slot `SiteScopeBar` de `@minimalart/mercatto-plugin-runtime@^0.2.0` — la barra reaparece en `/app/dynamic-groups` con la implementación del host. Fase B del pattern.

## 1.3.0 - Initial migration from base extension. All functionality preserved from packages/extensions/dynamic-groups/ at parity.
