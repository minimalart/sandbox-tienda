import type { MedusaContainer } from '@medusajs/framework/types';
export default function processGiftCardDeliveries(container: MedusaContainer): Promise<void>;
export declare const config: {
    name: string;
    schedule: string;
};
