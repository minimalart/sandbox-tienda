// GENERADO por packages/project-composer (renderSpaceDesignerSlot).
// No editar a mano: lo reescribe `pnpm site:components:extract` y `pnpm site:create`.
export { getSpaceConfigurators } from '@lib/data/space-designer';
